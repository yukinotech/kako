{
  "name": "kako",
  "version": "0.1.0",
  "description": "Media steganography CLI for hiding files in JPG/MP4 via EOF appending",
  "type": "module",
  "private": true,
  "module": "src/index.ts",
  "bin": {
    "kako": "./src/index.ts"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "clean": "bun run scripts/clean.ts",
    "build:bin": "bun run scripts/build-local.ts",
    "build:release:bin": "bun run scripts/build-release.ts",
    "build:verify": "bun run scripts/verify-local.ts",
    "build": "bun run clean && bun run build:bin",
    "build:release": "bun run clean && bun run build:release:bin",
    "build:full": "bun run build && bun run build:verify"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
